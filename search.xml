<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>树状数组的养成方法</title>
    <url>/2021/01/30/binary%20index%20tree/</url>
    <content><![CDATA[<p><strong>现在的我，能成为你的女主角吗。——《路人女主的养成方法》</strong></p>
<h1 id="树状数组"><a href="#树状数组" class="headerlink" title="树状数组"></a>树状数组</h1><p>$Binary\ Index\ Tree$</p>
<p><strong>树状数组（Binary Index Tree，BIT）</strong> 是一种时间复杂度为  $O(\log_2n)$ 的数据结构。</p>
<p>这篇文章主要介绍树状数组的三种基础操作：</p>
<p><strong>1、单点修改</strong></p>
<p><strong>2、区间查询</strong></p>
<p><strong>3、区间修改</strong></p>
<p>树状数组巧妙地利用了<strong>二进制</strong>。例如11，转化为二进制数就是 $(1011)_2$ ，如果我们要求前11项和，可以分别查询 $((0000)_2,(1000)_2]$ 、 $((1000)_2,(1010)_2]$ 以及 $((1010)_2,(1011)_2]$ 的和再相加。区间获取是一个不断地去掉二进制数最右边的一个1的过程。</p>
<p><img src="https://img.imgdb.cn/item/60149fd13ffa7d37b3aa8395.png" alt="我是BIT的过程！"></p>
<p>我们定义，二进制数最右边的一个1，连带着它之后的0为 $lowbit(x)$ <strong>（树状数组的核心）</strong>。那么我们用 $C_i$ 维护区间 $(A_i-lowbit(A_i),A_i]$ ，这样显然查询前n项和时需要合并的区间数是少于 $\log_2n$ 的，树状数组的结构大概像下面这样：</p>
<p><img src="https://img.imgdb.cn/item/6014a0193ffa7d37b3aa9364.png" alt="我是树状数组！"></p>
<h2 id="树状数组的实现"><a href="#树状数组的实现" class="headerlink" title="树状数组的实现"></a>树状数组的实现</h2><p><strong>1、单点修改/建树（板子）</strong></p>
<p>我个人比较喜欢 $while$ 形式的：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">add</span> <span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> k)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">while</span> (x &lt;= n)</span><br><span class="line">	&#123;</span><br><span class="line">		tree[x] += k;</span><br><span class="line">		x += lowbit(x);</span><br><span class="line">	&#125;</span><br><span class="line">    </span><br><span class="line">	<span class="keyword">return</span> ;</span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>
<p>也可以写成 $for$ 循环形式的：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">add</span> <span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> k)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = x; i &lt;= n; i += lowbit (x))</span><br><span class="line">	&#123;</span><br><span class="line">		tree[i] += k;</span><br><span class="line">	&#125;</span><br><span class="line">    </span><br><span class="line">	<span class="keyword">return</span> ;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>2、求前n项的和（板子）</strong></p>
<p>这边也有 $while$ 和 $for$ 不同的写法，但是意思都是一样的</p>
<p>$while$ 写法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">query</span> <span class="params">(<span class="keyword">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> ans = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">while</span> (x != <span class="number">0</span>)</span><br><span class="line">	&#123;</span><br><span class="line">		ans += tree[x];</span><br><span class="line">		x -= lowbit(x);</span><br><span class="line">	&#125;</span><br><span class="line">    </span><br><span class="line">	<span class="keyword">return</span> ans;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>$for$ 循环写法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">query</span> <span class="params">(<span class="keyword">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> ans = <span class="number">0</span>; </span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = x; i; i -= lowbit (i))</span><br><span class="line">	&#123;</span><br><span class="line">		ans += tree[i];</span><br><span class="line">	&#125;</span><br><span class="line">    </span><br><span class="line">	<span class="keyword">return</span> ans;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>3、区间修改，给 $[x,y]$ 的每个数 $+k$（前缀和+板子）</strong></p>
<p>特殊的是，当我们求 $[x,y]$ 的和时，需要用到<strong>前缀和</strong>思想（这里引用到模板题里一位大佬的题解）</p>
<p>具体代码这里就不贴了，$add$ 函数就是上面的板子。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">add (x, k);</span><br><span class="line">add (y + <span class="number">1</span>, - k);</span><br></pre></td></tr></table></figure>
<p>这种方法很巧妙，先给 $[x,n]$ 的每个数加$k$，再给 $[y+1,n]$ 的每个数加$-k$。</p>
<p>那么当然也可以实现区间求和，就是利用前缀和优化上面“前 $n$ 项和”的板子</p>
<p>总结：树状数组在<strong>单点查询</strong>和<strong>单点修改</strong>上还是可以挥发出很大作用的（指码量），但是更推荐线段树，毕竟树状数组能做的事情，线段树都能做。</p>
<p>$\large End.$</p>
]]></content>
      <categories>
        <category>OI知识</category>
      </categories>
      <tags>
        <tag>树状结构</tag>
        <tag>数据结构</tag>
        <tag>提高</tag>
      </tags>
  </entry>
  <entry>
    <title>网络流奇谭</title>
    <url>/2021/02/06/network-flows/</url>
    <content><![CDATA[<p><strong>世间万物就这样与我们毫无关联地独自变化；一眨眼，一低头，景色就会立刻不一样。没有觉察到这一点的话，就感觉很可惜呢。——《此花亭奇谭》</strong></p>
<h1 id="网络流"><a href="#网络流" class="headerlink" title="网络流"></a>网络流</h1><p>$network-flows$</p>
<p><strong>不$\ \large\star\ $明$\ \large\star\ $意$\ \large\star\ $义的解释</strong></p>
<p>网络流的话，你可以把它想象成是一个城市水管网络，自来水厂（<strong>源点</strong>）的水通过纵横交错的水管网络和很多中转站（中间的点）到达你家（<strong>汇点</strong>），而这些水管有粗有细，能通过这些水管的水（<strong>流量</strong>）也不同，而我们要求的就是最终到达你家的最大水流。</p>
<h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><p><strong>源点</strong> $\textbf{(S)}$ ：整个图的起点，源点的流量是<strong>无限大</strong> 的。</p>
<p><strong>汇点</strong> $\textbf{(T)}$ ：整张图的终点，<strong>流量为</strong> $0$。 </p>
<p><del>（都到终点了还要什么流量，难道你也住在水管里？）</del></p>
<p><strong>流量</strong> $\textbf{(flow)}$ ：$\forall (u,v)\in E$，一般用 $f(u,v)$ 表示。</p>
<p><strong>容量网络</strong>：设有向图 $G =(V,E) $ ，在点集 $V$ 中指定源点 $S$ 和汇点 $T\ (S\ne T)$ ，对于每条边 $(u,v)\in E$有权值 $C$。</p>
<p><strong>网络流</strong>：边集 $E$ 上流量集合 $ F = { \  \forall(u,v) \in E \ f(u,v)\  } $ 。</p>
<p><strong>可行流</strong>：若一条边对每一起点满足总流出量与总流入量之差不大于提供能力，对每一终点满足总流入量与总流出量之差不小于接收能力，则称这个流为可行流。即 $\forall (u,v)\in E, f(u,v)\leq C(u,v)$。</p>
<p><strong>增广路（这个非常重要）</strong>：从源点到汇点路径中一条交替联通两个集合的路径，其边上的流量均大于零。</p>
<p><strong>残留网络（这个也非常重要）</strong>：如果一张图，已经找过增广路了（并且流量已被用过），那这你就可以把它当成是残留网络，但是残留网络里还是可以走路径的（仍可以进行增广）。且每一次增广都在残留网络中进行，直到没有增广路为止。</p>
<p><strong>切割（显然，这个同样很重要）</strong>： $G=(V,E)$ 的割是 $[S,V-S]$，即把点集划分 $S$ 和 $V-S$ 两个部分。</p>
<h2 id="网络流的开始"><a href="#网络流的开始" class="headerlink" title="网络流的开始"></a>网络流的开始</h2><p>好差不多了，我们来看张图片。</p>
<p><img src="https://img.imgdb.cn/item/601cc88f3ffa7d37b3ca5524.jpg" alt="我是一个网络流的图"></p>
<p>假设我们从 $1 \to 2 \to 3 \to 4$ 走的话，得到的结果是 $1$ ，因为边 $2 \to 3$ 的流量为 $0$ ，你无法再从 $1 \to 3 \to 2$ 到达汇点 $4$ 了。</p>
<p><img src="https://img.imgdb.cn/item/601cc88f3ffa7d37b3ca5526.jpg" alt="我是一个残余网络"></p>
<p>但是这样得到结果并不优， $1 \to 2 \to 4$ 和 $1 \to 3 \to 4$ 得到的结果是 $2$（流到你家的水变多了！）， $2$ 才是我们要得到的解。</p>
<p>基于这个想法，我们可以给每条边加上一个反向边。这样就做到了如果不是最优解，还能跑回去更新一遍最优解，相当于给了程序一个反悔的机会。</p>
<h1 id="常用算法"><a href="#常用算法" class="headerlink" title="常用算法"></a>常用算法</h1><p>接下来是一些常用的网络流算法。</p>
<h2 id="Ford-Fulkerson"><a href="#Ford-Fulkerson" class="headerlink" title="$Ford-Fulkerson$"></a>$Ford-Fulkerson$</h2><p><del>这是一个很差劲的算法，是副标题吸引你划下来的</del></p>
<p>时间复杂度： $O(nm^2)$</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">dfs</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> flow)</span> <span class="comment">// dfs求任意路径，x记当前搜索点，flow记这条边的流量 </span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(x == t) <span class="comment">// 仍可以走到 </span></span><br><span class="line">    &#123;</span><br><span class="line">        ans += flow; <span class="comment">// 加上本次增广得到的流量  </span></span><br><span class="line">        flag = <span class="number">1</span>; <span class="comment">// 标记为可以找到 </span></span><br><span class="line">        <span class="keyword">return</span> flow; <span class="comment">// 返回流量，接下来尝试去找更优解</span></span><br><span class="line">    &#125;</span><br><span class="line">    mark[x] = <span class="number">1</span>; <span class="comment">// 做标记</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = f[x].head; i != <span class="number">-1</span>; i = f[i].next) <span class="comment">// 链式前向星遍历 </span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> x1 = f[i].to; <span class="comment">// 记录这条边的终点 </span></span><br><span class="line">        <span class="keyword">if</span>(mark[x1] || f[i].b == <span class="number">0</span>) </span><br><span class="line">	    <span class="keyword">continue</span>; <span class="comment">// 这条边已经走过，或者这条边的流量为0（已经被榨干了） </span></span><br><span class="line">        <span class="keyword">int</span> delta = dfs(x1, min(flow, f[i].b)); <span class="comment">// 给dfs传入这条增广路的最小流量</span></span><br><span class="line">        <span class="keyword">if</span>(flag == <span class="literal">true</span>) <span class="comment">// 首先我找到了增广路，再尝试更新一遍寻找最优解 </span></span><br><span class="line">        &#123;</span><br><span class="line">            f[i].b -= delta; <span class="comment">// 减去我本次得到的流量 </span></span><br><span class="line">            f[i^<span class="number">1</span>].b += delta; <span class="comment">// 给反向路加上 </span></span><br><span class="line">            <span class="keyword">return</span> delta; <span class="comment">// 依旧返回 </span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>; <span class="comment">// 找不到了，此时会返回false，结束函数 </span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">FF</span><span class="params">()</span><span class="comment">//有增广路就增广</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">memset</span>(mark, <span class="number">0</span>, <span class="keyword">sizeof</span>(mark));<span class="comment">//初始化标记 </span></span><br><span class="line">    flag = <span class="literal">false</span>;</span><br><span class="line">    dfs(s, INF); <span class="comment">// INF表示流量为0</span></span><br><span class="line">    <span class="keyword">while</span>(flag == <span class="literal">true</span>) <span class="comment">// 确保还有路可以增广 </span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">memset</span>(mark, <span class="number">0</span>, <span class="keyword">sizeof</span>(mark)); <span class="comment">// 初始化标记，每次都需要 </span></span><br><span class="line">        flag = <span class="literal">false</span>; <span class="comment">// 先做false标记 </span></span><br><span class="line">        dfs(s, INF); <span class="comment">// 那么如果没有增广路，就会退出 </span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注释里应该讲得挺清楚的了，可以去自己模拟一下，利用 $dfs$ 一路走到底，如果可以通到汇点，那就加上流量，然后再用反向边来找一下更优解。</p>
<p>可是这真是太慢了。</p>
<p>$FF$ 未吸氧（不要在意我的背景）</p>
<p><img src="https://img.imgdb.cn/item/601d1f163ffa7d37b3f038b4.png" alt="显然很慢"></p>
<p>$FF + O_2$</p>
<p><img src="https://img.imgdb.cn/item/601d1f163ffa7d37b3f038b6.png" alt="显然这连模板题都过不了啊"></p>
<p>就算是吸了氧，得到的结果也是大大的 $TLE$ ，唯一的区别可能就是内存小了点。</p>
<p>既然可以使用 $dfs$ ，为什么不可以用 $bfs$ + 队列试试看呢</p>
<p>开搞</p>
<h2 id="Edmonds-Karp"><a href="#Edmonds-Karp" class="headerlink" title="$Edmonds-Karp$"></a>$Edmonds-Karp$</h2><p><del>这是一个不那么差劲的算法，你是被我拉下来的（诶，难道不是因为好奇吗！）</del></p>
<p>时间复杂度： $O(n^2m)$</p>
<p>上代码！</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">bfs</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">memset</span>(vis, <span class="number">0</span>, <span class="keyword">sizeof</span>(vis)); <span class="comment">// 初始化标记数组 </span></span><br><span class="line">    <span class="built_in">queue</span>&lt;<span class="keyword">int</span>&gt; q; <span class="comment">// 新建一个队列 </span></span><br><span class="line">    q.push(s); <span class="comment">// 将源点推入队列 </span></span><br><span class="line">    vis[s] = <span class="number">1</span>; <span class="comment">//标记源点为已走过 </span></span><br><span class="line">    incf[s] = inf; <span class="comment">// 源点流量为无限 </span></span><br><span class="line">    <span class="keyword">while</span>(q.size()) <span class="comment">// 只要队列不为空就继续增广，这里同样可以写成q.empty() </span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> now = q.front(); <span class="comment">// 取出队列中的第一个元素</span></span><br><span class="line">	q.pop(); <span class="comment">// 并删除掉它（过河拆桥） </span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = head[now]; i; i = e[i].nxt) <span class="comment">// 老规矩前向星遍历 </span></span><br><span class="line">	&#123;</span><br><span class="line">            <span class="keyword">if</span>(e[i].w == <span class="number">0</span> || vis[e[i].v]) <span class="keyword">continue</span>; <span class="comment">// 如果这条边已被标记或没有流量就剪枝 </span></span><br><span class="line">            <span class="keyword">int</span> to = e[i].v; <span class="comment">// 记录这条边的终点 </span></span><br><span class="line">            incf[to] = min(incf[now], e[i].w); <span class="comment">// 记录当前增广的路径的最小流量 </span></span><br><span class="line">            pre[to] = i; <span class="comment">// 记录路径边的编号（方便增广） </span></span><br><span class="line">            q.push(to); <span class="comment">// 将这条边的终点推入队列准备找增广路 </span></span><br><span class="line">            vis[to] = <span class="number">1</span>; <span class="comment">// 记录这个点为走过</span></span><br><span class="line">            <span class="keyword">if</span>(to == t) </span><br><span class="line">	    <span class="keyword">return</span> <span class="number">1</span>; <span class="comment">// 走到汇点就直接退出（都说了你家不是水管！） </span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>用 $bfs$ 分层式的找增广路，用反向边找最优解，那这个算法应该很好吧。</p>
<p><del>其实，我还是觉得很不舒服。</del></p>
<p>看结果，加不加 $O2$ 没有什么区别</p>
<p><img src="https://img.imgdb.cn/item/601d24d03ffa7d37b3f2203c.png" alt="最后几个点卡过去了！！"></p>
<p>还有没有更好的算法呢？（对啊有没有更好的算法呢？）</p>
<p>有，这种算法还是EK算法提出者提出的。</p>
<h2 id="Dinic"><a href="#Dinic" class="headerlink" title="$Dinic$"></a>$Dinic$</h2><p><del>这是一个比较好的算法，是我让你过来的，你必须得下来看看这个。</del></p>
<p>我们试着使用了 $dfs$ 和 $bfs$ 来寻找增广路，这个算法倒好，集聚两家的智慧，用 $bfs$ 分层，用 $dfs$ 来寻找。</p>
<p>这种算法还可以用“多路增广”和“当前弧优化”来进行<del>简单</del>地优化算法（我的代码里已使用当前弧优化）</p>
<p>首先是 $bfs$ 部分的代码</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">bfs</span> <span class="params">()</span> <span class="comment">// 广搜分层 </span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">memset</span> (d, <span class="number">-1</span>, <span class="keyword">sizeof</span> (d)); <span class="comment">// 初始化深度（分层） </span></span><br><span class="line">	<span class="built_in">queue</span>&lt;ll&gt; q; <span class="comment">// 新建一个队列 </span></span><br><span class="line">	q.push (s); <span class="comment">// 将源点推入队列 </span></span><br><span class="line">	d[s] = <span class="number">0</span>; <span class="comment">// 源点的深度为0 </span></span><br><span class="line">	cur[s] = head[s]; <span class="comment">//  获取源点指向的第一个点（由源点扩展出去） </span></span><br><span class="line">	<span class="keyword">while</span> (q.size ()) <span class="comment">// 队列不为空 </span></span><br><span class="line">	&#123;</span><br><span class="line">		ll u = q.front (); <span class="comment">// 取出队头 </span></span><br><span class="line">		q.pop (); <span class="comment">// 删掉队头 </span></span><br><span class="line">		<span class="keyword">for</span> (ll i = head[u]; i; i = Next[i]) <span class="comment">// 前向星遍历 </span></span><br><span class="line">		&#123;</span><br><span class="line">			ll v = ver[i]; <span class="comment">// 取出终点 </span></span><br><span class="line">			<span class="keyword">if</span> (d[v] == <span class="number">-1</span> &amp;&amp; f[i]) <span class="comment">// 如果深度未被标记，且这条路上仍有流量 </span></span><br><span class="line">			&#123;</span><br><span class="line">				q.push (v); <span class="comment">// 将这个终点推入队列 </span></span><br><span class="line">				d[v] = d[u] + <span class="number">1</span>; <span class="comment">// 这个点的深度是这条边的起点加 1 </span></span><br><span class="line">				cur[v] = head[v]; <span class="comment">// 遍历时取出这条边的终点 </span></span><br><span class="line">				<span class="keyword">if</span> (v == t) <span class="comment">// 如果已经搜到汇点 </span></span><br><span class="line">					<span class="keyword">return</span> <span class="literal">true</span>; <span class="comment">// 结束一次增广 </span></span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>示意图大概是这样：</p>
<p><img src="https://img.imgdb.cn/item/601f35bd3ffa7d37b3c4ef5a.png" alt="分层图我就想到分层设色地形图www"></p>
<p>注释应该很清楚了，可以自己手动模拟一下</p>
<p>然后是 $dfs$ 部分：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">dfs</span> <span class="params">(<span class="keyword">int</span> u, <span class="keyword">int</span> limit)</span> <span class="comment">// u是当前搜索点，limit是这条边上的瓶颈边权（即这条路上的最小流量） </span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (u == t) <span class="comment">// 搜到汇点，递归结束 </span></span><br><span class="line">		<span class="keyword">return</span> limit; <span class="comment">// 返回这条边上的最小值（瓶颈边） </span></span><br><span class="line">	<span class="keyword">int</span> flow = <span class="number">0</span>; <span class="comment">// flow是我当前走这条增广路获得的流量 </span></span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = cur[u]; i &amp;&amp; flow &lt; limit; i = Next[i]) <span class="comment">// 遍历 </span></span><br><span class="line">	&#123;</span><br><span class="line">		cur[u] = i; <span class="comment">// 取出起点 </span></span><br><span class="line">		ll v = ver[i]; <span class="comment">// 取出终点 </span></span><br><span class="line">		<span class="keyword">if</span> (f[i] != <span class="number">0</span> &amp;&amp; d[v] == d[u] + <span class="number">1</span>) <span class="comment">// 确保这条边的流量不为零且深度是加一的 </span></span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">int</span> fl = dfs (v, min (f[i], limit - flow)); <span class="comment">// 深搜下去 </span></span><br><span class="line">			<span class="keyword">if</span> (!fl) <span class="comment">// 这条流量不为零 </span></span><br><span class="line">				d[v] = <span class="number">-1</span>; <span class="comment">// 记录深度为不可走 </span></span><br><span class="line">			f[i] -= fl; <span class="comment">// 减去流量 </span></span><br><span class="line">			f[i ^ <span class="number">1</span>] += fl; <span class="comment">// 反向边操作 </span></span><br><span class="line">			flow += f; <span class="comment">// 加在这条增广路上获得的流量和上 </span></span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> flow; <span class="comment">// 返回这条增广路上得到的流量 </span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>注意，有一段代码很难理解，这边我讲一下（如果能自行理解就请跳过这一段，因为我被这个东西搞了一天）</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">dfs(v, min(f[i], limit - flow)</span><br></pre></td></tr></table></figure>
<p>诶这段话就很有意思，传给下一次深搜的值是<strong>一个终点</strong>和<strong>一个最小值</strong>，二者分别是<strong>这条边的当前流量（容量）</strong>，和<strong>当前获取流量减去最小值</strong>。</p>
<p>来看一张图：</p>
<p><img src="https://img.imgdb.cn/item/601e0b213ffa7d37b34861a2.png" alt="卡了我两天的问题"></p>
<p>很明显，最终得到的答案应该是 $10$ ，而不是两条增广路上的瓶颈边值 $11$ 。</p>
<p>我们可以走 $1 \to 2 \to T$ 和 $1 \to 3 \to T$ 来得到最大流，但是整个过程是<strong>动态的</strong>，我们不应该只看当前图上的 $3$ 和 $8$ （虽然现在这是两条增广路上的最小流）。但在经过上面那条增广路时， $S \to 1$ 的流量会减少 $3$ 。</p>
<p><img src="https://img.imgdb.cn/item/601e0c863ffa7d37b348f55b.png" alt="实际上是这样，请不要搞混淆"></p>
<p>现在这个时候，上面那条路走不了了（因为没有流量），只能从下面走，而下面这条路此时的瓶颈边变成了 $S \to 1$ ，而最小流就只能是 $7$ ，最终得到的最大流就是 $3+7=10$ ，反则亦之。</p>
<p>不过还是要和容量比较的，因为可能容量是较小值，那这时候只能按容量来算了。</p>
<p>在程序中这样调用：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">dinic</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> maxflow = <span class="number">0</span>, flow = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">while</span> (bfs ()) <span class="comment">// 找到增广路</span></span><br><span class="line">		<span class="keyword">while</span> (flow = dfs (s, inf)) <span class="comment">// 开始增广</span></span><br><span class="line">			maxflow += flow; <span class="comment">// 加上本次增广得到的流量</span></span><br><span class="line">	<span class="keyword">return</span> maxflow;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>重复进行一个“找增广路，往增广路走，更新最优解，增加最大流”的过程。</p>
<h1 id="例题"><a href="#例题" class="headerlink" title="例题"></a>例题</h1><p>这里是一些有意思的网络流题目。</p>
<h2 id="P2740-USACO4-2-Drainage-Ditches"><a href="#P2740-USACO4-2-Drainage-Ditches" class="headerlink" title="$P2740\ [\  USACO4.2\ ]\ Drainage\ Ditches$"></a>$P2740\ [\  USACO4.2\ ]\ Drainage\ Ditches$</h2><p><del>这是一道非常经典的网络流题目，是你自己划下来看的。</del></p>
<p><a href="https://www.luogu.com.cn/problem/P2740">%题目の传送门%</a></p>
<p>这道题目和模板题几乎一模一样了吧……想法很简单，就是让我们算水管的最大流，从 $1$ 点流到 $n$ 点的最大流就是答案，那不也就是贴个板子的事。</p>
<p>代码（板子，由某大佬的代码改成）：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstdio&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;queue&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstring&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">long</span> ll;</span><br><span class="line"><span class="keyword">const</span> ll N = <span class="number">210</span>, M = <span class="number">10010</span>, inf = <span class="number">1</span> &lt;&lt; <span class="number">30</span>;</span><br><span class="line">ll n, m, s, t, tot;</span><br><span class="line">ll head[N], c[M], Next[M], ver[M];</span><br><span class="line">ll d[N], cur[N];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">add</span> <span class="params">(ll x, ll y, ll  z)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	c[++tot] = z, ver[tot] = y, Next[tot] = head[x], head[x] = tot;</span><br><span class="line">	c[++tot] = <span class="number">0</span>, ver[tot] = x, Next[tot] = head[y], head[y] = tot;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">bfs</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">memset</span> (d, <span class="number">-1</span>, <span class="keyword">sizeof</span> (d));</span><br><span class="line">	<span class="built_in">queue</span>&lt;ll&gt; q;</span><br><span class="line">	q.push (s);</span><br><span class="line">	d[s] = <span class="number">0</span>;</span><br><span class="line">	cur[s] = head[s];</span><br><span class="line">	<span class="keyword">while</span> (q.size ())</span><br><span class="line">	&#123;</span><br><span class="line">		ll u = q.front ();</span><br><span class="line">		q.pop ();</span><br><span class="line">		<span class="keyword">for</span> (ll i = head[u]; i; i = Next[i])</span><br><span class="line">		&#123;</span><br><span class="line">			ll v = ver[i];</span><br><span class="line">			<span class="keyword">if</span> (d[v] == <span class="number">-1</span> &amp;&amp; c[i])</span><br><span class="line">			&#123;</span><br><span class="line">				q.push (v);</span><br><span class="line">				d[v] = d[u] + <span class="number">1</span>;</span><br><span class="line">				cur[v] = head[v];</span><br><span class="line">				<span class="keyword">if</span> (v == t)</span><br><span class="line">					<span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function">ll <span class="title">dfs</span> <span class="params">(ll u, ll limit)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (u == t)</span><br><span class="line">		<span class="keyword">return</span> limit;</span><br><span class="line">	ll flow = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span> (ll i = cur[u]; i &amp;&amp; flow &lt; limit; i = Next[i])</span><br><span class="line">	&#123;</span><br><span class="line">		cur[u] = i;</span><br><span class="line">		ll v = ver[i];</span><br><span class="line">		<span class="keyword">if</span> (c[i] != <span class="number">0</span> &amp;&amp; d[v] == d[u] + <span class="number">1</span>)</span><br><span class="line">		&#123;</span><br><span class="line">			ll f = dfs (v, min (c[i], limit - flow));</span><br><span class="line">			<span class="keyword">if</span> (!f)</span><br><span class="line">				d[v] = <span class="number">-1</span>;</span><br><span class="line">			c[i] -= f, c[i ^ <span class="number">1</span>] += f;</span><br><span class="line">			flow += f;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> flow;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function">ll <span class="title">dinic</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	ll maxflow = <span class="number">0</span>, flow = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">while</span> (bfs ())</span><br><span class="line">		<span class="keyword">while</span> (flow = dfs (s, inf))</span><br><span class="line">			maxflow += flow;</span><br><span class="line">	<span class="keyword">return</span> maxflow;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">scanf</span>(<span class="string">&quot;%d %d&quot;</span>, &amp;m, &amp;n);</span><br><span class="line">	s=<span class="number">1</span>, t=n;</span><br><span class="line">	tot = <span class="number">1</span>;</span><br><span class="line">	<span class="keyword">while</span> (m --)</span><br><span class="line">	&#123;</span><br><span class="line">		ll u, v, c;</span><br><span class="line">		<span class="built_in">scanf</span>(<span class="string">&quot;%d %d %d&quot;</span>, &amp;u, &amp;v, &amp;c);</span><br><span class="line">		add (u, v, c);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; dinic () &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="P1402-酒店之王"><a href="#P1402-酒店之王" class="headerlink" title="$P1402$ 酒店之王"></a>$P1402$ 酒店之王</h2><p><a href="https://www.luogu.com.cn/problem/P1402">%题目の传送门%</a></p>
<p>噢这道题目非常的好（在看过题解之后）</p>
<p>代码应该很简单，主要是<strong>建图的思想</strong>。</p>
<p>这题是可以用两个二分图匹配做的，但是我还是毅然决然地选择了使用 $Dinic$ 算法。</p>
<p>先是<strong>建图</strong>，然后就是贴板子的小事情（我贴个板子的事就弄了一早上）</p>
<p>第一，如果第 $i$ 个人喜欢第 $j$ 个房间，就连一条从“人”点到“房间”点的边，关于“菜”也是这样的。然后为了防止重复计算，我们要把一个人分成两个人（他自己和他的影分身）</p>
<p><img src="https://img.imgdb.cn/item/6020cdbb3ffa7d37b37e3085.png" alt="这样会被重复跑"></p>
<p>解决方案就是<strong>把这个点拆开来</strong>，<strong>这样中间就只有一条路了</strong></p>
<p><img src="https://img.imgdb.cn/item/6020cdbb3ffa7d37b37e3088.png" alt="拆点，这样就可以了"></p>
<p>然后建边的时候注意点的编号次序，这很容易搞错，可以自己在纸上画一下。</p>
<p>代码给出：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> N 1000001</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> int long long</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> n, p, q, S, T;</span><br><span class="line"><span class="keyword">int</span> cnt = <span class="number">1</span>, head[N], deep[N], cur[N];</span><br><span class="line"><span class="keyword">int</span> k;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Edge</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">int</span> to, f, nxt;</span><br><span class="line">&#125;edge[N];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">add_edge</span> <span class="params">(<span class="keyword">int</span> u, <span class="keyword">int</span> v, <span class="keyword">int</span> w)</span> <span class="comment">// 建边 </span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	edge[++cnt].f = w;</span><br><span class="line">	edge[cnt].to = v;</span><br><span class="line">	edge[cnt].nxt = head[u];</span><br><span class="line">	head[u] = cnt;</span><br><span class="line">	<span class="keyword">return</span> ; </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">bool</span> <span class="title">bfs</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">memset</span> (deep, <span class="number">-1</span>, <span class="keyword">sizeof</span> (deep));</span><br><span class="line">	<span class="built_in">queue</span>&lt;<span class="keyword">int</span>&gt; q;</span><br><span class="line">	q.push (S);</span><br><span class="line">	deep[S] = <span class="number">0</span>;</span><br><span class="line">	cur[S] = head[S];</span><br><span class="line">	<span class="keyword">while</span> (!q.empty ())</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">int</span> u = q.front ();</span><br><span class="line">		q.pop ();</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = head[u]; i; i = edge[i].nxt)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">int</span> v = edge[i].to;</span><br><span class="line">			<span class="keyword">if</span> (deep[v] == <span class="number">-1</span> &amp;&amp; edge[i].f)</span><br><span class="line">			&#123;</span><br><span class="line">				q.push (v);</span><br><span class="line">				deep[v] = deep[u] + <span class="number">1</span>;</span><br><span class="line">				cur[v] = head[v];</span><br><span class="line">				<span class="keyword">if</span> (v == T)</span><br><span class="line">					<span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">dfs</span> <span class="params">(<span class="keyword">int</span> u, <span class="keyword">int</span> limit)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (u == T)</span><br><span class="line">		<span class="keyword">return</span> limit;</span><br><span class="line">	<span class="keyword">int</span> flow = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = cur[u]; i &amp;&amp; flow &lt; limit; i = edge[i].nxt)</span><br><span class="line">	&#123;</span><br><span class="line">		cur[u] = i;</span><br><span class="line">		<span class="keyword">int</span> v = edge[i].to;</span><br><span class="line">		<span class="keyword">if</span> (edge[i].f != <span class="number">0</span> &amp;&amp; deep[v] == deep[u] + <span class="number">1</span>)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">int</span> f = dfs (v, min (edge[i].f, limit - flow));</span><br><span class="line">			<span class="keyword">if</span> (!f)</span><br><span class="line">				deep[v] = <span class="number">-1</span>;</span><br><span class="line">			edge[i].f -= f, edge[i ^ <span class="number">1</span>].f += f;</span><br><span class="line">			flow += f;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">return</span> flow;</span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">dinic</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;    </span><br><span class="line">	<span class="keyword">int</span> maxflow = <span class="number">0</span>, flow = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">while</span> (bfs ())</span><br><span class="line">		<span class="keyword">while</span> (flow = dfs (S, N))</span><br><span class="line">			maxflow += flow;</span><br><span class="line">			</span><br><span class="line">	<span class="keyword">return</span> maxflow;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">signed</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">scanf</span> (<span class="string">&quot;%lld %lld %lld&quot;</span>, &amp;n, &amp;p, &amp;q);</span><br><span class="line">	S = <span class="number">1</span>;</span><br><span class="line">	T = n * <span class="number">2</span> + p + q + <span class="number">2</span>; <span class="comment">// 总共的点数是 n * 2 + p + q + 2 </span></span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i ++) <span class="comment">// 给 房间 --&gt; 人本体 建边 </span></span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">1</span>; j &lt;= p; j ++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="built_in">scanf</span> (<span class="string">&quot;%lld&quot;</span>, &amp;k);</span><br><span class="line">			<span class="keyword">if</span> (k == <span class="number">1</span>)</span><br><span class="line">			&#123;</span><br><span class="line">				add_edge (j + <span class="number">1</span>, <span class="number">1</span> + p + i, <span class="number">1</span>); <span class="comment">// 第 i 个人 喜欢 第 j 个房间 </span></span><br><span class="line">				add_edge (<span class="number">1</span> + p + i, j + <span class="number">1</span>, <span class="number">0</span>); <span class="comment">// 建反向边 </span></span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i ++) <span class="comment">// 给 人分身 --&gt; 菜 建边 </span></span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">1</span>; j &lt;= q; j ++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="built_in">scanf</span> (<span class="string">&quot;%lld&quot;</span>, &amp;k);</span><br><span class="line">			<span class="keyword">if</span> (k)</span><br><span class="line">			&#123;</span><br><span class="line">				add_edge (<span class="number">1</span> + p + n + i, <span class="number">1</span> + p + n * <span class="number">2</span> + j, <span class="number">1</span>); <span class="comment">// 第 i 个人 喜欢 第 j 道菜 </span></span><br><span class="line">				add_edge (<span class="number">1</span> + p + n * <span class="number">2</span> + j, <span class="number">1</span> + p + n + i, <span class="number">0</span>); <span class="comment">// 建反向边 </span></span><br><span class="line">			&#125;</span><br><span class="line">		&#125;	</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= p; i ++) <span class="comment">// 给 源点 --&gt; 房间 建边 </span></span><br><span class="line">	&#123;</span><br><span class="line">		add_edge (<span class="number">1</span>, <span class="number">1</span> + i, <span class="number">1</span>);</span><br><span class="line">		add_edge (<span class="number">1</span> + i, <span class="number">1</span>, <span class="number">0</span>); <span class="comment">// 建反向边 </span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= q; i ++) <span class="comment">// 给 菜 --&gt; 汇点 建边 </span></span><br><span class="line">	&#123;</span><br><span class="line">		add_edge (<span class="number">1</span> + p + n * <span class="number">2</span> + i, <span class="number">2</span> + n * <span class="number">2</span> + p + q, <span class="number">1</span>);</span><br><span class="line">		add_edge (<span class="number">2</span> + n * <span class="number">2</span> + p + q, <span class="number">1</span> + p + n * <span class="number">2</span> + i, <span class="number">0</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i ++) <span class="comment">// 给 人本体 --&gt; 人分身 建边 </span></span><br><span class="line">	&#123;</span><br><span class="line">		add_edge (<span class="number">1</span> + p + i, <span class="number">1</span> + p + n + i, <span class="number">1</span>); <span class="comment">// 第 i 个人 喜欢 第 i 个人（你也是屑魔女吗） </span></span><br><span class="line">		add_edge (<span class="number">1</span> + p + n + i, <span class="number">1</span> + p + i, <span class="number">0</span>); <span class="comment">// 建反向边 </span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; dinic () &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这种建边思想可以利用到别的网络流题目里去。</p>
<p>然后之后可能会补充一些最小费用最大流的知识点，等我更完《最短之路》再说（因为我还没有学qwq）。</p>
<p>这边补充一个链式前向星建图的代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">add_edge</span> <span class="params">(<span class="keyword">int</span> u, <span class="keyword">int</span> v, <span class="keyword">int</span> w)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	edge[++ cnt] = (Edge) &#123;v, head[u], w&#125;; </span><br><span class="line">	head[u] = cnt;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这是从一个大佬那边看到的，完全可以背板子，比前面的写得好</p>
<p>去暴切网络流 $24$ 题吧！！！</p>
<p>这里挂一下我的个人题单《网络流奇谭》：</p>
<p><a href="https://www.luogu.com.cn/training/55215">https://www.luogu.com.cn/training/55215</a></p>
<p>$\large End.$</p>
]]></content>
      <categories>
        <category>OI知识</category>
      </categories>
      <tags>
        <tag>图论</tag>
        <tag>省选</tag>
      </tags>
  </entry>
</search>
